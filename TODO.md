function to implement for this branch
- [x] job posting, (simply listing, addition and deleting need to be manually done by some admin)
- [ ] job posting, adding jobs only if user.role == "company"
- [x] profiles, adding profile for user.role == "user" and (resumes, this needs to check the file upload Apis and the url)